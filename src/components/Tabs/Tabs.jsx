import React, { useState } from "react";
import styled from "styled-components";
const StyledTabs = styled.div``;

function Tabs(props) {
  //   const [fetch, setFetch] = useState();
  console.log("RENDER TABS");
  return <StyledTabs></StyledTabs>;
}
export default Tabs;
